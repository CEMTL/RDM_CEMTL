@model Solution.ViewModels.UnitesOrganisationnelles.UniteOrganisationnelleFormViewModel
@{
    ViewBag.Title = "Modifier Département";
}

<div class="page-header-custom">
    <h2>Modifier UniteOrganisationnelle</h2>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.UniteOrganisationnelleId)
    @Html.Partial("_Form", Model)

    <div class="dashboard-card">
        <button type="submit" class="btn btn-success">Enregistrer</button>
        @Html.ActionLink("Retour", "Index", null, new { @class = "btn btn-default" })
    </div>
}

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")
}