@model Solution.ViewModels.JournalImports.JournalImportFormViewModel
@{
    ViewBag.Title = "Modifier JournalImport";
}

<div class="page-header-custom">
    <h2>Modifier JournalImport</h2>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.JournalImportId)
    @Html.Partial("_Form", Model)

    <div class="dashboard-card">
        <button type="submit" class="btn btn-success">Enregistrer</button>
        @Html.ActionLink("Retour", "Index", null, new { @class = "btn btn-default" })
    </div>
}

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")
}