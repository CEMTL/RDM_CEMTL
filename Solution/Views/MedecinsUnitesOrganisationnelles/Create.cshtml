@model Solution.ViewModels.MedecinsUnitesOrganisationnelles.MedecinUniteOrganisationnelleFormViewModel
@{
    ViewBag.Title = "Créer MedecinUniteOrganisationnelle";
}

<div class="page-header-custom">
    <h2>Créer MedecinUniteOrganisationnelle</h2>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.MedecinUniteOrganisationnelleId)
    @Html.Partial("_Form", Model)

    <div class="dashboard-card">
        <button type="submit" class="btn btn-success">Enregistrer</button>
        @Html.ActionLink("Retour", "Index", null, new { @class = "btn btn-default" })
    </div>
}

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")
}