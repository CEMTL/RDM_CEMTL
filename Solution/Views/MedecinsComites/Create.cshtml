@model Solution.ViewModels.MedecinsComites.MedecinComiteFormViewModel
@{
    ViewBag.Title = "Créer MedecinComite";
}

<div class="page-header-custom">
    <h2>Créer MedecinComite</h2>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.MedecinComiteId)
    @Html.Partial("_Form", Model)

    <div class="dashboard-card">
        <button type="submit" class="btn btn-success">Enregistrer</button>
        @Html.ActionLink("Retour", "Index", null, new { @class = "btn btn-default" })
    </div>
}

@section Scripts {
    @Html.Partial("_ValidationScriptsPartial")
}